<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.modeldownloader">

    <!-- Permission to access the internet for downloading the model -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppGlobals"
        android:label="Model Downloader"
        android:allowBackup="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity to guide the user through provider and model selection. -->
        <activity
            android:name=".ProviderSelectionActivity"
            android:exported="false" />
    </application>

</manifest>